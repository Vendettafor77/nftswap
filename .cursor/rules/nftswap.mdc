---
description: 
globs: 
alwaysApply: true
---
# 项目进度与规则

## 当前进度检查

- **合约部分**：已完成编写与测试。
- **UI设计**：正在进行调整和调试，计划通过本地节点即时观察并修改。
- **前端文件**：位于 `frontend` 文件夹下，需检查现有进度。
- **ethers 中间层**：已开始编写。
- **IPFS 导入逻辑**：尚未开始。

## 下一步行动

1. **检查现有文件**：全面检查 `frontend` 文件夹下的所有文件，明确每个函数、功能、组件的作用，并检查根目录 `README` 和 `TASKS` 的现有描述是否准确。
2. **UI设计**：检查 UI，确保包含所有合约功能，并使用日语作为用户可见部分的语言。

## 修改流程

- **每次修改**：不超过两个文件。
- **功能修改**：先全面检查功能的导入、组件、实现方式等，修改后必须同步更新 `README` 和 `TASKS`。

## 注意事项

- **前端语言**：用户可见部分全部使用日语。
- **回答语言**：全部使用中文，专用名词加上日语翻译。
- **强制规则检查**：每次回答前必须检查所有规则，确保完全遵守，尤其是关于文档更新和语言使用的要求。
